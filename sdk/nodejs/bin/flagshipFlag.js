"use strict";
// *** WARNING: this file was generated by pulumi-language-nodejs. ***
// *** Do not edit by hand unless you're certain you know what you are doing! ***
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.FlagshipFlag = void 0;
const pulumi = __importStar(require("@pulumi/pulumi"));
const utilities = __importStar(require("./utilities"));
/**
 * Accepted Permissions
 *
 * - `Flagship Read`
 * - `Flagship Write`
 *
 * ## Example Usage
 *
 * ```typescript
 * import * as pulumi from "@pulumi/pulumi";
 * import * as cloudflare from "@pulumi/cloudflare";
 *
 * const exampleFlagshipFlag = new cloudflare.FlagshipFlag("example_flagship_flag", {
 *     accountId: "account_id",
 *     appId: "app_id",
 *     defaultVariation: "x",
 *     enabled: true,
 *     key: "x",
 *     rules: [{
 *         conditions: [{
 *             attribute: "x",
 *             operator: "equals",
 *             value: {},
 *         }],
 *         priority: 1,
 *         serveVariation: "x",
 *         rollout: {
 *             percentage: 0,
 *             attribute: "x",
 *         },
 *     }],
 *     variations: {
 *         foo: "string",
 *     },
 *     description: "description",
 *     type: "boolean",
 * });
 * ```
 *
 * ## Import
 *
 * > This resource does not currently support `pulumi import`.
 */
class FlagshipFlag extends pulumi.CustomResource {
    /**
     * Get an existing FlagshipFlag resource's state with the given name, ID, and optional extra
     * properties used to qualify the lookup.
     *
     * @param name The _unique_ name of the resulting resource.
     * @param id The _unique_ provider ID of the resource to lookup.
     * @param state Any extra arguments used during the lookup.
     * @param opts Optional settings to control the behavior of the CustomResource.
     */
    static get(name, id, state, opts) {
        return new FlagshipFlag(name, state, { ...opts, id: id });
    }
    /** @internal */
    static __pulumiType = 'cloudflare:index/flagshipFlag:FlagshipFlag';
    /**
     * Returns true if the given object is an instance of FlagshipFlag.  This is designed to work even
     * when multiple copies of the Pulumi SDK have been loaded into the same process.
     */
    static isInstance(obj) {
        if (obj === undefined || obj === null) {
            return false;
        }
        return obj['__pulumiType'] === FlagshipFlag.__pulumiType;
    }
    constructor(name, argsOrState, opts) {
        let resourceInputs = {};
        opts = opts || {};
        if (opts.id) {
            const state = argsOrState;
            resourceInputs["accountId"] = state?.accountId;
            resourceInputs["appId"] = state?.appId;
            resourceInputs["defaultVariation"] = state?.defaultVariation;
            resourceInputs["description"] = state?.description;
            resourceInputs["enabled"] = state?.enabled;
            resourceInputs["flagKey"] = state?.flagKey;
            resourceInputs["key"] = state?.key;
            resourceInputs["rules"] = state?.rules;
            resourceInputs["type"] = state?.type;
            resourceInputs["updatedAt"] = state?.updatedAt;
            resourceInputs["updatedBy"] = state?.updatedBy;
            resourceInputs["variations"] = state?.variations;
        }
        else {
            const args = argsOrState;
            if (args?.accountId === undefined && !opts.urn) {
                throw new Error("Missing required property 'accountId'");
            }
            if (args?.appId === undefined && !opts.urn) {
                throw new Error("Missing required property 'appId'");
            }
            if (args?.defaultVariation === undefined && !opts.urn) {
                throw new Error("Missing required property 'defaultVariation'");
            }
            if (args?.enabled === undefined && !opts.urn) {
                throw new Error("Missing required property 'enabled'");
            }
            if (args?.key === undefined && !opts.urn) {
                throw new Error("Missing required property 'key'");
            }
            if (args?.rules === undefined && !opts.urn) {
                throw new Error("Missing required property 'rules'");
            }
            if (args?.variations === undefined && !opts.urn) {
                throw new Error("Missing required property 'variations'");
            }
            resourceInputs["accountId"] = args?.accountId;
            resourceInputs["appId"] = args?.appId;
            resourceInputs["defaultVariation"] = args?.defaultVariation;
            resourceInputs["description"] = args?.description;
            resourceInputs["enabled"] = args?.enabled;
            resourceInputs["flagKey"] = args?.flagKey;
            resourceInputs["key"] = args?.key;
            resourceInputs["rules"] = args?.rules;
            resourceInputs["type"] = args?.type;
            resourceInputs["variations"] = args?.variations;
            resourceInputs["updatedAt"] = undefined /*out*/;
            resourceInputs["updatedBy"] = undefined /*out*/;
        }
        opts = pulumi.mergeOptions(utilities.resourceOptsDefaults(), opts);
        super(FlagshipFlag.__pulumiType, name, resourceInputs, opts);
    }
}
exports.FlagshipFlag = FlagshipFlag;
//# sourceMappingURL=flagshipFlag.js.map